{
  "name": "google-old-favicons-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that restores old-style Google app favicons in open tabs.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
